class APIManager:
    def __call__(self, option_1, option_2, args):
        """
        Function to act as the option to function mapping
        params option 1: First input from user
        params option 2: Second input from user, depends on param 1
        """
        if option_1==1:
            # It has three more hidden option
            if option_2==1:
                # get the file name
            elif option_2==2:
            elif option_2==3:
        elif option_1==2:
        elif option_1==3:
        elif option_1==4:
        elif option_1==5:
        elif option_1==6:
            # It has three more hidden option
            if option_2==1:
                # get the file name
            elif option_2==2:
        elif option_1==7:
        elif option_1==8:

        # List is used to convert to tuple
        return tuple([arg1, arg2]), is_continue

        